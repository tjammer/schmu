(rule
 (targets std.smi std.o array.smi array.o)
 (deps std.smu %{bin:schmu} array.smu)
 (action
  (progn
   (run schmu -m --no-std array.smu)
   (run schmu -m --no-std std.smu))))

(install
 (section
  (site
   (schmu std)))
 (files std.smu std.smi std.o array.smu array.smi array.o))
