(library
 (name schmulang)
 (preprocess
  (pps ppx_deriving.show))
 (libraries
  llvm
  llvm.analysis
  llvm.target
  llvm.all_backends
  llvm.passmgr_builder
  llvm.scalar_opts))

(menhir
 (modules parser)
 (flags --explain))

(ocamllex
 (modules lexer))
