(library
 (name schmulang)
 (preprocess
  (pps ppx_deriving.show ppx_deriving.eq))
 (libraries llvm llvm.analysis llvm.target llvm.all_backends))

(menhir
 (modules parser)
 (flags --explain))

(ocamllex
 (modules lexer))
