type big = { a : int, b : float, c : int, d : int }

fun do_something(big) = big.a + 1

-- do_something({a = 10, b = 12.0, c = 12, d = 13})

external printf : (cstr, int) -> unit

fun printi(i) = printf("%i\n".cstr, i)

vector_iter(Malloc_some.vtest, printi)
