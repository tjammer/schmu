ignore(Some(1))

type foo = {x : float, y : float}
type larger = One | Two(foo) | Three(int)

ignore(Three(3))

type clike = A | B | C | D | E

ignore(C)

fun wrap_clike() { C }

fun wrap_option() {Some(copy("hello"))}

0
