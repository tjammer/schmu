type option('a) = None, Some('a)

Some(1) |> ignore

type foo = { x : float, y : float }
type larger = One, Two(foo), Three(int)

Three(3) |> ignore

type clike = A, B, C, D, E

C |> ignore

fun wrap_clike() = C

fun wrap_option() = Some("hello")

0
