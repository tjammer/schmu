let mut a = 10
match mut a {
  mut b -> mut b = 11
}
fmt/(println(int, a))

type record = {mut a : int, b : float}
let mut a = Some(10)
match mut a {
  Some(mut b) -> mut b = 12
  None -> ()
}
match a {
  Some(a) -> fmt/(println(int, a))
  None -> println("none")
}
