let v& = [0]
let const = copy(v.[0])
&v.[0] = 1
print(fmt(v.[0]))
print(fmt(const))

fun in_fun():
  let v& = [0]
  let const = copy(v.[0])
  &v.[0] = 1
  print(fmt(v.[0]))
  print(fmt(const))

in_fun()
