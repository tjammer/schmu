type f = {mut a : int}

fun modify(mut r) {mut r.a = 30}

fun mod2(mut a) {array/push(mut a, mov 20)}

let mut a = {a = 20}
modify(mut a)
fmt/(println(int, a.a))

let mut b = [10]
mod2(mut b)
fmt/(println(int, array/length(b)))
