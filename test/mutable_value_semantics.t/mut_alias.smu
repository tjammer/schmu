(type foo {:a& int})

(def f {:a 0})
(def fst& f)
(def snd fst)
(set &(.a fst) 1)
(print (fmt-str (.a fst)))
(print (fmt-str (.a f)))
(print (fmt-str (.a snd)))

(defn new-fun []
  (def f {:a 0})
  (def fst& f)
  (def snd fst)
  (set &(.a fst) 1)
  (print (fmt-str (.a fst)))
  (print (fmt-str (.a f)))
  (print (fmt-str (.a snd))))

(new-fun)
