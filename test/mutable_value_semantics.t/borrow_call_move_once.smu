fun higher_order(once fn) { fn(); () }

fun test(a!) {
  higher_order (fun () {
    -- free a at end of scope
    ignore((a, ()))
  })
}

test(copy("10"))

-- TODO param count
-- fun test(a!) {
--   let () <-higher_order ()
--   -- free a at end of scope
--   ignore((a, ()))
-- }

-- test(copy("10"))
