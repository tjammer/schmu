fun ret_fn(b):
  fun bla(a): a + b
  bla

fun ret_lambda(b):
  fun a: a + b

let f = ret_fn(13)
let f2 = ret_fn(35)
println(fmt(f(12)))
println(fmt(f2(12)))

let f = ret_lambda(134)
println(fmt(f(12)))
