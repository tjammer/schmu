-- (mod/extr-a {:b 10})

(rec
 (fun even? [i]
   (match i
     (0 true)
     (1 false)
     (n (odd? (- n 1)))))
 (fun odd? [i]
   (match i
     (0 false)
     (1 true)
     (n (even? (- n 1))))))

-- (print (fmt-str (odd? 1)))
-- (print (fmt-str (odd? 100)))
-- (print (fmt-str (even? 100)))

(val cnt& 0)

(rec
 (fun pop [arr&]
   (print "pop")
   (set &cnt (+ cnt 1))
   (array-drop-back &arr)
   (pop-all &arr))
 (fun pop-all [arr&]
   (if (= (array-length arr) 0)
     ()
     (pop &arr))))

-- (val arr& [0 1 2 3])
-- (pop-all &arr)
-- (print (fmt-str (array-length arr)))

(fun test [i]
  (print "wrong")
  i)

(fun test [i]
  (print "right")
  i)
