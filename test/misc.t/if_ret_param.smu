(def s "/")

(defn times [limit f]
  (defn inner [i]
    (if (= i limit)
      ()
      (do
        (f s)
        (inner (+ i 1)))))
  (inner 0))

(defn test [value]
  (ignore (if true
            value
            (fmt-str "/" value))))
(times 2 test)
