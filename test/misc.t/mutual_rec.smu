(rec
 (defn even? (i)
   (match i
     (0 true)
     (1 false)
     (n (odd? (- n 1)))))
 (defn odd? (i)
   (match i
     (0 false)
     (1 true)
     (n (even? (- n 1))))))

(print (fmt-str (odd? 1)))
(print (fmt-str (odd? 100)))
(print (fmt-str (even? 100)))
