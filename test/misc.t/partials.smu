(type (f 'a) {:a 'a :b 'a :c 'a})

(defn inf []
  (def a {:a [10] :b [10] :c [10]})
  (def _ !(if true
    (if false a.c a.a)
    -- a.a
    -- [10]
    a.c))
  a.b)

(ignore (inf))

(type (t 'a) {:a& 'a})
(defn set-moved ()
  (def a& {:a [10]})
  (ignore {a.a})
  (set &a.a [20])
  )

(set-moved)
