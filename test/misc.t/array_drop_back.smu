let nested& = [[0, 1], [2, 3]]
print(fmt(Array.length(nested)))
Array.drop_back(&nested)
print(fmt(Array.length(nested)))
Array.drop_back(&nested)
print(fmt(Array.length(nested)))
Array.drop_back(&nested)
print(fmt(Array.length(nested)))
