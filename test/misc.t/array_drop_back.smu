let nested& = [[0, 1], [2, 3]]
println(fmt(array/length(nested)))
array/drop_back(&nested)
println(fmt(array/length(nested)))
array/drop_back(&nested)
println(fmt(array/length(nested)))
array/drop_back(&nested)
println(fmt(array/length(nested)))
