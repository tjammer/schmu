type segment = {start : float, width : float}

type bar = {start : float, end : float, nsegs : int,
            margin : float, y : float, thick : f32}

let height = 720

let world = {
  start = 0.0,
  end = 1280.0,
  nsegs = 10,
  margin = 0.1,
  y = float_of_int(height) *. 0.75,
  thick = 5.0f32}

fun get_seg(bar) {ignore(bar)}

fun wrap_seg() {get_seg(world)}

wrap_seg()

0
