(type segment {:start float :width float})

(type bar {:start float :end float :nsegs int :margin float :y float :thick f32})

(def height 720)

(def world {:start 0.0
            :end 1280.0
            :nsegs 10
            :margin 0.1
            :y (*. (float_of_int height) 0.75)
            :thick 5.0f32})

(defn get-seg
  "Get segment [i] of [bar]"
  [bar]
  (ignore bar))

(defn wrap-seg []
  (get-seg world))

(wrap-seg)

0
