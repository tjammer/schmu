(defn ret-none ()
  (def [a (option (array int))] #none)
  a)
(def b !(match (ret-none) ((#some a) a) (#none [1 2])))
(ignore b)

(type (r 'a) {:a 'a})
(defn ret-rec () {:a [10 20 30]})
(def c (let ((a (ret-rec))) (.a a).[1]))
(ignore c)
