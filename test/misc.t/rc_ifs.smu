(defn test [b]
  (def (ret (array int)) [])
  (defn inner [i]
    (def (ai (array int)) [])
    (def (bi (array int)) [])
    (if (= i b)
      ai
     (if (= i 30)
       bi
       (inner (+ i 1)))))
  (def io (inner 10))
  (ignore io)
  ret)


(def a (test 2))
(ignore a)
