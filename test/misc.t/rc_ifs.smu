(fun test [b]
  (val (ret (array int)) [])
  (fun inner [i]
    (val (ai (array int)) [])
    (val (bi (array int)) [])
    (if (= i b)
      ai
     (if (= i 30)
       bi
      (do
         (inner (+ i 1))))))
  (val io (inner 10))
  (ignore io)
  ret)


(val a (test 2))
(ignore a)
