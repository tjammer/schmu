type int_wrap = { dat: int, b: int, c: int }
type test('a)  = { int_wrap: 'a }

fun test_thing()
  test = { int_wrap = { dat = 2, b = 0, c = 0 } }
  fun vector_loop(i)
    if i == 10 then
      test.int_wrap
    else
      vector_loop(i+1)
    end
  end
  vector_loop(0)
end

test_thing() .dat
