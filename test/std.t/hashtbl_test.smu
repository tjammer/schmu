(module stbl (hashtbl/make string))

(defn string []
  (print "## string")
  (def tbl& (stbl/create 64))
  (stbl/insert &tbl !"key" !1.1)
  (defn find-print [key]
    (match (stbl/find tbl key)
      ((#some v) (print (fmt-str v)))
      (#none (print "none"))))
  (find-print "key")
  (find-print "otherkey"))

(print "# hashtbl")
(string)
