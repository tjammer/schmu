let a = 'a'

module A:
  type t = int
  print("i'm in a module")
  module A:
    type t = float
    print(fmt(a))
    assert(u8_to_int(a) == u8_to_int('a'))
    let a = 10
    print(fmt(a))
    assert(a == 10)

print(fmt(a))

import A

print(fmt(a))
print(fmt(A.a))
