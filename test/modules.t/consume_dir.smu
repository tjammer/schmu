import modd
use modd

println(a)
println(b)

println(modd/public/a)

modd/func()
println(modd/lambda())
println(modd/closure())

-- use polymoorphic aliased function from another module
fun swapped(a!, i, j):
  let a& = !a
  swap(&a, i, j)
  a

array/iter(swapped(!["world", "hello"], 0, 1), fun s: println(s))
