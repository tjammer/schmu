module type sig {
  type t
  val hash : (t) -> int
  val to_string : (t) -> string
}

functor make(m : sig) {
  type other['a] = {this : m/t, other : 'a}

  fun create(this!, other!, fmt) {
    fmt/print2("create: {} {}\n", fmt/str, m/to_string(this), fmt, other)
    {this, other}
  }
}
