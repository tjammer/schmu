let a = "modd"

let b = Indirect.a

module Public = Public

-- This should not raise an llvm warning
let func = Indirect.hmm

let lambda = Indirect.some_lambda

-- Use closure directly
let closure = Indirect.some_closure

let swap = Indirect.swap
