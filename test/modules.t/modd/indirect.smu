let a = "indirect"

fun hmm():
  array/iter([], fun(i : int): ignore(i))

let some_lambda = do:
  let a = "lol"
  fun (): copy(a)

let some_closure = do:
  let a = fmt("lol")
  fun (): copy(a)

-- polymorphic alias
let swap = array/swap_items
