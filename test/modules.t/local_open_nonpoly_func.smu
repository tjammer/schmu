external printf : (cstr, int) -> unit

fun do(a) = Nonpoly_func.(match a with
  -- TODO open modules for patterns
  Left -> add_ints(0, 5)
  Right -> add_ints(0, -5)
)

do(Nonpoly_func.Left)  |> printf("%i\n".cstr)
