(open lets)

(printf (string/data "%i\n") a)
(printf (string/data "%i\n") b)

(defn inside-fn []
  (open lets)
  (defn second []
    (printf (string/data "%i\n") a)
    (printf (string/data "%i\n") b)  )
  (second))

(inside-fn)
