let thing = __rc(![0, 1])
let _ = copy(thing)

let variant = __rc(!#some("string"))
copy(variant).ignore()

type thing = { a : option(array(int)) }
let record = __rc(!{a = #some([1])})
copy(record).ignore()
