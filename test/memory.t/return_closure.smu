fun ret_fn(b) {
  fun bla(a) {a + b}
  bla
}

fun ret_lambda(b) {
  fun a {a + b}
}

let f = ret_fn(13)
let f2 = ret_fn(35)
fmt/(println(int, f(12)))
fmt/(println(int, f2(12)))

let f = ret_lambda(134)
fmt/(println(int, f(12)))
