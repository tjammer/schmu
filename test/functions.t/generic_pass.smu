external printi : int -> unit

type t = { i : int, b : bool }

fn pass(x)
  x

fn apply(f, x)
  f(x)

-- printi(apply(pass, 20)) >>
20->>apply(pass)->printi >>
-- printi(apply(pass, { i = 700, b = false }).i)
{ i = 700, b = false }->>apply(pass).i->printi
