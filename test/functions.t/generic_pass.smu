(external printi (fun int unit))

(type t {:i int :b bool})

(fun pass [x] x)

(fun apply [f x] (f x))

-- printi(apply(pass, 20))
(-> (apply pass 20)
    printi)
-- printi(apply(pass, { i = 700, b = false }).i)
(->> {:i 700 :b false}
        (apply pass)
        (.i)
        printi)
