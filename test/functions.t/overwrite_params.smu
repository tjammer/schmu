external printi : int -> unit

-- b is captured by two()
b = 2

fun one() 1 end

fun two() b end

fun add(a, b)
  -- b should be a function here, not captured value
  a() + b()
end

printi(add(one, two))
