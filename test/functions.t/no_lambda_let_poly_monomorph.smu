(external printi (fun int unit))

(record t {:x int})
-- A polymorphic anonymous function which can not be resolved in the backend (yet)
(val f (if true (fun [x] x) (fun [x] x)))

(printi (.x (f {:x 17})))
