external printi : int -> unit

type t = { x : int }

-- A polymorphic anonymous function which can not be resolved in the backend (yet)
f = if true then fn (x) x end else fn (x) x end end

printi(f({ x = 17 }).x)
