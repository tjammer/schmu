fn one() -> int
  1

fn add(a : int, b : int) -> int
  a + b

fn doiflesselse(a : int, b : int, greater : int, less : int)
  if a < b then
    less
  else
    greater

doiflesselse(add(one(), 1), 0, 1, 2)
