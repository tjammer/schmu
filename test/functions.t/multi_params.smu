function one() -> int
  1

function add(a : int, b : int) -> int
  a + b

function doiflesselse(a : int, b : int, greater : int, less : int)
  if a < b then
    less
  else
    greater

doiflesselse(add(one(), 1), 0, 1, 2)
