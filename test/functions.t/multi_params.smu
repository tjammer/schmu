fun one() -> int 1 end

fun add(a : int, b : int) -> int
  a + b
end

fun doiflesselse(a : int, b : int, greater : int, less : int)
  if a < b then less
  else greater end
end

doiflesselse(add(one(), 1), 0, 1, 2)
